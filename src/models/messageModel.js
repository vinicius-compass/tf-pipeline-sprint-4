//const axios = require("axios");

const messageModel = {
    getMessage: () => {
        const response = "Este é o app do Grupo 01 😀";
        return response;
    },
};

module.exports = messageModel;
